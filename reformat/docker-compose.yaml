services:
  reformat:
    build:
      context: ..
      dockerfile: reformat/Dockerfile
    container_name: reformat-code
    volumes:
      - ./pytest_mcp_tools/src:/workspace/src:rw
    working_dir: /workspace
    network_mode: bridge
