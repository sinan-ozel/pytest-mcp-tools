# Annotations Server

MCP test server with tools that have proper annotations.

Each tool has a `title`, and `readOnlyHint`, `idempotentHint`, and
`destructiveHint` are set consistently (no conflicts).
